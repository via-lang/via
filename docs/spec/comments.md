# Comments

via uses generic C-style comments.

## Line comments

Line comments are prefixed with `//` and continue until the end of the current line.

```
// This is a comment
```

## Multi-line comments

Multi-line comments are prefixed with `/*` and suffixed with `*/`.

```
/*
This is a multi-line comment!
*/
```
