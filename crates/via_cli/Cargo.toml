[package]
name = "via_cli"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.100"
clap = { version = "4.5.54", features = ["derive"] }
clio = { version = "0.3.5" , features = ["clap-parse"] }
via_core = { path = "../via_core" }
